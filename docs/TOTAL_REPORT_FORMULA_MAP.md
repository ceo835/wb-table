# TOTAL_REPORT_FORMULA_MAP

## Что такое лист Итого

- `Итого` — это не raw-выгрузка, а широкая сводная витрина Excel с примесью ручных полей, wide-pivot блоков и локальных формул.
- Большая часть структуры листа завязана на другие вкладки workbook, а не на единый нормализованный источник.
- Для database-first архитектуры лист надо разложить на: `fact_*` таблицы источников, `dim_product`, и итоговую витрину `mart_total_report`.

## Главные формульные паттерны

### Базовая воронка и KPI

- `Показы`: `=VLOOKUP(B22,'Воронка на день'!$B$2:$BO$1048,8,0)`. Точечный lookup в лист `Воронка на день` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `Переходы в карточку`: `=VLOOKUP(B22,'Воронка на день'!$B$2:$BO$1048,14,0)`. Точечный lookup в лист `Воронка на день` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `Положили в корзину`: `=VLOOKUP(B22,'Воронка на день'!$B$2:$BO$1048,16,0)`. Точечный lookup в лист `Воронка на день` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `Заказали, шт`: `=VLOOKUP(B22,'Воронка на день'!$B$2:$BO$1048,20,0)`. Точечный lookup в лист `Воронка на день` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `СиТиАр`: `=E3/D3`. Локальный расчет отношения двух метрик на листе Итого.

### РасходРК и эффективность корзин

- `Затраты РК/Раньше было (Реальная корзина)`: `=IFERROR(VLOOKUP(U104,'РасходРК'!$K$2:$L$65,2,0),0)`. Точечный lookup в лист `РасходРК` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `корзин от этой РК (эффективность РК)`: `=IFERROR(VLOOKUP(U104,'РасходРК'!$A$95:$T$748,8,0),0)`. Точечный lookup в лист `РасходРК` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `CTR корзины`: `=IFERROR(VLOOKUP(U104,'РасходРК'!$A$95:$T$748,20,0),"")`. Точечный lookup в лист `РасходРК` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `цена корзин от этой РК (эффективность РК)`: `=IFERROR(V104/W104,"")`. Локальный расчет отношения двух метрик на листе Итого.
- `Показы РК этого артикула`: `=IFERROR(VLOOKUP(U104,'РасходРК'!$A$95:$T$748,6,0),0)`. Точечный lookup в лист `РасходРК` по nm_id/заголовку с жестко зафиксированным диапазоном.

### ВБро

- `='ВБро'!D2`: `=VLOOKUP($B34,'ВБро'!$A$2:$E$79,4,0)`. Точечный lookup в лист `ВБро` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `='ВБро'!E2`: `=VLOOKUP($B34,'ВБро'!$A$2:$E$79,5,0)`. Точечный lookup в лист `ВБро` по nm_id/заголовку с жестко зафиксированным диапазоном.

### Точка входа: каталоги и поверхности

- `Подборки и рекомендации (Карточка)`: `=SUMIFS('Точка вх'!$H:$H, 'Точка вх'!$C:$C, $B37, 'Точка вх'!$A:$A, "Карточка товара", 'Точка вх'!$B:$B, "Подборки и рекомендации")`. Агрегация через SUMIF/SUMIFS по данным листа `Точка вх` с фиксированными диапазонами.
- `Кнопка «Похожие»`: `=SUMIFS('Точка вх'!$H:$H, 'Точка вх'!$C:$C, $B37, 'Точка вх'!$A:$A, "Карточка товара", 'Точка вх'!$B:$B, "Кнопка «Похожие»")`. Агрегация через SUMIF/SUMIFS по данным листа `Точка вх` с фиксированными диапазонами.
- `Рекомендации продавца`: `=SUMIFS('Точка вх'!$H:$H, 'Точка вх'!$C:$C, $B37, 'Точка вх'!$A:$A, "Карточка товара", 'Точка вх'!$B:$B, "Рекомендации продавца")`. Агрегация через SUMIF/SUMIFS по данным листа `Точка вх` с фиксированными диапазонами.
- `Бренд и его категории`: `=SUMIFS('Точка вх'!$H:$H, 'Точка вх'!$C:$C, $B37, 'Точка вх'!$A:$A, "Карточка товара", 'Точка вх'!$B:$B, "Бренд и его категории")`. Агрегация через SUMIF/SUMIFS по данным листа `Точка вх` с фиксированными диапазонами.
- `Блок «Ещё предложения»`: `=SUMIFS('Точка вх'!$H:$H, 'Точка вх'!$C:$C, $B37, 'Точка вх'!$A:$A, "Карточка товара", 'Точка вх'!$B:$B, "Блок «Ещё предложения»")`. Агрегация через SUMIF/SUMIFS по данным листа `Точка вх` с фиксированными диапазонами.

### Сравнение карточек / MPStat

- `Рейтинг карточки`: `=VLOOKUP(CQ$1, 'Сравнение карточек'!$A$2:$V$40, MATCH($CP44, 'Сравнение карточек'!$A$2:$W$2, 0), FALSE)`. Точечный lookup в лист `Сравнение карточек` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `Рейтинг по отзывам`: `=VLOOKUP(CR$1, 'Сравнение карточек'!$A$2:$V$40, MATCH($CP44, 'Сравнение карточек'!$A$2:$W$2, 0), FALSE)`. Точечный lookup в лист `Сравнение карточек` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `Среднее время доставки`: `=VLOOKUP(CS$1, 'Сравнение карточек'!$A$2:$V$40, MATCH($CP44, 'Сравнение карточек'!$A$2:$W$2, 0), FALSE)`. Точечный lookup в лист `Сравнение карточек` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `Средняя позиция`: `=VLOOKUP(CT$1, 'Сравнение карточек'!$A$2:$V$40, MATCH($CP44, 'Сравнение карточек'!$A$2:$W$2, 0), FALSE)`. Точечный lookup в лист `Сравнение карточек` по nm_id/заголовку с жестко зафиксированным диапазоном.
- `Показы`: `=VLOOKUP(CU$1, 'Сравнение карточек'!$A$2:$V$40, MATCH($CP44, 'Сравнение карточек'!$A$2:$W$2, 0), FALSE)`. Точечный lookup в лист `Сравнение карточек` по nm_id/заголовку с жестко зафиксированным диапазоном.

### Локализация по регионам

- `Дальневосточный и Сибирский`: `=SUMIFS('Локализация'!$J:$J, 'Локализация'!$C:$C, $B62, 'Локализация'!$F:$F,DE$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Локализация` с фиксированными диапазонами.
- `Уральский`: `=SUMIFS('Локализация'!$J:$J, 'Локализация'!$C:$C, $B62, 'Локализация'!$F:$F,DF$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Локализация` с фиксированными диапазонами.
- `Приволжский`: `=SUMIFS('Локализация'!$J:$J, 'Локализация'!$C:$C, $B62, 'Локализация'!$F:$F,DG$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Локализация` с фиксированными диапазонами.
- `Северо-Западный`: `=SUMIFS('Локализация'!$J:$J, 'Локализация'!$C:$C, $B62, 'Локализация'!$F:$F,DH$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Локализация` с фиксированными диапазонами.
- `Центральный`: `=SUMIFS('Локализация'!$J:$J, 'Локализация'!$C:$C, $B62, 'Локализация'!$F:$F,DI$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Локализация` с фиксированными диапазонами.

### РК стата / матрица кампаний

- `Кот женский Арт. 36387055  АРК Т/Б +`: `=IF(SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DT$1, 'РасходРК'!$C$96:$C$748, $B104)=0,"",SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DT$1, 'РасходРК'!$C$96:$C$748, $B104))`. Агрегация через SUMIF/SUMIFS по данным листа `РасходРК` с фиксированными диапазонами.
- `Буст Арт. 221311710 черный 7 = Т/Б +`: `=IF(SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DU$1, 'РасходРК'!$C$96:$C$748, $B104)=0,"",SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DU$1, 'РасходРК'!$C$96:$C$748, $B104))`. Агрегация через SUMIF/SUMIFS по данным листа `РасходРК` с фиксированными диапазонами.
- `Буст Арт. 91744473 раб мин Т/Б +`: `=IF(SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DV$1, 'РасходРК'!$C$96:$C$748, $B104)=0,"",SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DV$1, 'РасходРК'!$C$96:$C$748, $B104))`. Агрегация через SUMIF/SUMIFS по данным листа `РасходРК` с фиксированными диапазонами.
- `Буст Арт. 139345207 Авокадо мин ставка Т/Б +`: `=IF(SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DW$1, 'РасходРК'!$C$96:$C$748, $B104)=0,"",SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DW$1, 'РасходРК'!$C$96:$C$748, $B104))`. Агрегация через SUMIF/SUMIFS по данным листа `РасходРК` с фиксированными диапазонами.
- `Буст Арт. 279085455 очки Дубль Т/Б +`: `=IF(SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DX$1, 'РасходРК'!$C$96:$C$748, $B104)=0,"",SUMIFS('РасходРК'!$H$96:$H$748, 'РасходРК'!$B$96:$B$748, DX$1, 'РасходРК'!$C$96:$C$748, $B104))`. Агрегация через SUMIF/SUMIFS по данным листа `РасходРК` с фиксированными диапазонами.

### Локализация summary

- `всего корзин`: `=SUM(MC104:AWJ104)`. Локальная бизнес-формула, которую нужно перенести в mart_total_report после явной спецификации.

### Поисковые запросы

- `трусы женские`: `=SUMIFS('Поисковые запросы'!$T:$T,'Поисковые запросы'!$B:$B,'Итого'!$B72,'Поисковые запросы'!$H:$H,'Итого'!MC$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Поисковые запросы` с фиксированными диапазонами.
- `трусы женские набор`: `=SUMIFS('Поисковые запросы'!$T:$T,'Поисковые запросы'!$B:$B,'Итого'!$B72,'Поисковые запросы'!$H:$H,'Итого'!MD$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Поисковые запросы` с фиксированными диапазонами.
- `трусы женские набор хлопок`: `=SUMIFS('Поисковые запросы'!$T:$T,'Поисковые запросы'!$B:$B,'Итого'!$B72,'Поисковые запросы'!$H:$H,'Итого'!ME$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Поисковые запросы` с фиксированными диапазонами.
- `трусы женские набор черные`: `=SUMIFS('Поисковые запросы'!$T:$T,'Поисковые запросы'!$B:$B,'Итого'!$B72,'Поисковые запросы'!$H:$H,'Итого'!MF$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Поисковые запросы` с фиксированными диапазонами.
- `трусы женские хлопок`: `=SUMIFS('Поисковые запросы'!$T:$T,'Поисковые запросы'!$B:$B,'Итого'!$B72,'Поисковые запросы'!$H:$H,'Итого'!MG$1)`. Агрегация через SUMIF/SUMIFS по данным листа `Поисковые запросы` с фиксированными диапазонами.

## Зависимости по листам

- `Воронка на день`: базовый источник по дням и nm_id для показов, переходов, корзин, заказов, CTR и части локальных KPI.
- `РасходРК`: источник рекламных расходов и wide-матрицы рекламных кампаний; часть полей Итого использует `SUMIF/SUMIFS` по этому листу.
- `РК стата`: в текущем xlsm служит как целевой рекламный блок, но в итоговой архитектуре должен читаться как `fact_ad_campaign_nm_day`; для production нужен лаг минимум D-2.
- `ВБро`: ручной внешний сервис; в Итого сейчас стоят прямые ссылки на лист `ВБро`, но автоматического расчета прибыли делать не нужно.
- `Точка вх`: широкий pivot по surfaces/customer profile; нужен CSV/Excel export или private endpoint.
- `Локализация`: wide-региональные показатели; текущий `region-sale` покрывает только часть задачи, целевой источник — `orders-geography`.
- `Сравнение карточек`: внешний comparison/MPStat блок, часть формул Итого читает его как справочник рейтингов и позиций.
- `Поисковые запросы`: wide-матрица поисковых фраз; в БД должна стать `fact_search_query_metric`, а конкурентные percentile поля остаются отдельной зоной подтверждения.

## Что уже можно перенести в БД

- Уже покрывается текущим API-пайплайном: базовая воронка, расходы рекламы, часть рекламной статистики, часть поисковых запросов, часть локализации.
- Требует manual/export: `ВБро`, `Точка вх`, часть `Локализация`, часть wide campaign/search blocks, которые в xlsm пока построены как Excel-pivot.
- Требует external service: `Сравнение карточек` / MPStat-подобные поля.
- Требует отдельного подтверждения: wide-формулы с `#REF!`, скрытая макросная логика и бизнес-смысл некоторых промежуточных KPI колонок.

## Риски при переносе

- Колонки с `#REF!`: `1`. Эти формулы нельзя переносить без ручной проверки.
- Колонки с фиксированными диапазонами: `2050`. Они сломаются при масштабировании списка артикулов/кампаний.
- В workbook есть `vbaProject.bin`: часть подготовки данных может выполняться макросами, что в этом анализе не исполнялось.